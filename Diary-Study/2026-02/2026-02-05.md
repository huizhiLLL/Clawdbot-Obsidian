# 学习日记 - 2026-02-05

## 主题：JS 对象操作

### 📚 核心知识点

#### 1. 对象的基本概念
对象是 JavaScript 中最重要的数据类型之一，它是一种键值对（key-value）的集合。

```javascript
// 创建对象
const person = {
  name: "会枝",
  age: 20,
  major: "机械工程"
};
```

#### 2. 对象属性访问

**点表示法（Dot Notation）**
```javascript
console.log(person.name); // "会枝"
console.log(person.age);  // 20
```

**方括号表示法（Bracket Notation）**
```javascript
console.log(person["name"]); // "会枝"
const key = "age";
console.log(person[key]);    // 20
```

**区别：**
- 点表示法更简洁，但键名必须是合法的标识符
- 方括号表示法更灵活，可以使用变量或特殊字符

#### 3. 对象解构

**基本解构**
```javascript
const { name, age } = person;
console.log(name); // "会枝"
console.log(age);  // 20
```

**重命名**
```javascript
const { name: userName, age: userAge } = person;
console.log(userName); // "会枝"
```

**默认值**
```javascript
const { city = "上海" } = person;
console.log(city); // "上海"（因为 person 中没有 city 属性）
```

**嵌套解构**
```javascript
const user = {
  name: "会枝",
  address: {
    city: "上海",
    district: "嘉定"
  }
};

const { address: { city, district } } = user;
console.log(city);    // "上海"
console.log(district); // "嘉定"
```

#### 4. 展开运算符（Spread Operator）

**对象展开**
```javascript
const person2 = { ...person, city: "上海" };
console.log(person2); // { name: "会枝", age: 20, major: "机械工程", city: "上海" }
```

**合并对象**
```javascript
const base = { x: 1, y: 2 };
const updates = { y: 20, z: 3 };
const merged = { ...base, ...updates };
console.log(merged); // { x: 1, y: 20, z: 3 }
```

**注意事项：后面的属性会覆盖前面的同名属性**

### 🔗 关联知识

- **数组解构**：`const [first, second] = [1, 2];`
- **函数参数解构**：`function createUser({ name, age }) { }`
- **不可变性**：展开运算符创建新对象，不修改原对象

### 💻 练习

1. **基础练习**
   ```javascript
   const student = {
     id: 1001,
     name: "张三",
     scores: { math: 90, english: 85 }
   };

   // 使用解构获取 name 和 math 成绩
   // 使用展开运算符创建一个新对象，添加 scores.cs: 88
   ```

2. **进阶练习**
   ```javascript
   const defaults = {
     theme: "light",
     language: "zh-CN",
     notifications: true
   };

   const userSettings = {
     language: "en-US"
   };

   // 合并 defaults 和 userSettings，用户设置优先
   ```

3. **实战练习**
   ```javascript
   function formatDate(date) {
     const { year, month, day } = date;
     return `${year}-${String(month).padStart(2, '0')}-${String(day).padStart(2, '0')}`;
   }

   console.log(formatDate({ year: 2026, month: 2, day: 5 }));
   // 输出: "2026-02-05"
   ```

### 📖 延伸阅读

- MDN: [使用对象](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Working_with_Objects)
- MDN: [解构赋值](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment)
- MDN: [展开语法](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/Spread_syntax)

---

**💡 晚上测验时间：22:00**

记得完成上面的练习，准备回答测验问题！

---

## 📝 测验问题

### 问题 1
给定以下对象：
```javascript
const config = {
  api: {
    endpoint: "https://api.example.com",
    timeout: 5000
  },
  retries: 3
};
```

请使用**嵌套解构**提取 `endpoint` 和 `retries`，并给 `endpoint` 重命名为 `url`。

### 问题 2
请解释以下代码的输出结果，并说明原因：
```javascript
const obj1 = { a: 1, b: 2 };
const obj2 = { b: 20, c: 3 };
const merged = { ...obj1, ...obj2 };
console.log(merged);
```

### 问题 3
什么情况下应该使用方括号表示法（`obj["key"]`）而不是点表示法（`obj.key`）？请举例说明。

---

## 🎯 学习进度记录

（等待用户回答后记录...）
