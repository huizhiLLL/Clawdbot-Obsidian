# 学习笔记 - 2026-02-15

## 📚 主题：Node.js 文件系统操作

### 🎯 学习目标
掌握 Node.js 中文件系统（fs）模块的基本操作，包括读取、写入和删除文件。

---

## 🔑 核心知识点

### 1. fs 模块导入

Node.js 提供了 `fs`（file system）模块用于文件操作。有两种方式导入：

```javascript
// 同步操作（常用）
const fs = require('fs');

// 异步操作（Promise 版本，推荐）
const fs = require('fs').promises;
```

### 2. 读取文件

#### 同步读取
```javascript
const fs = require('fs');

try {
  const data = fs.readFileSync('./file.txt', 'utf-8');
  console.log(data);
} catch (err) {
  console.error('读取失败:', err);
}
```

#### 异步读取（推荐）
```javascript
const fs = require('fs').promises;

async function readFile() {
  try {
    const data = await fs.readFile('./file.txt', 'utf-8');
    console.log(data);
  } catch (err) {
    console.error('读取失败:', err);
  }
}

readFile();
```

### 3. 写入文件

#### 同步写入
```javascript
const fs = require('fs');

fs.writeFileSync('./file.txt', 'Hello, World!', 'utf-8');
```

#### 异步写入（推荐）
```javascript
const fs = require('fs').promises;

async function writeFile() {
  try {
    await fs.writeFile('./file.txt', 'Hello, World!', 'utf-8');
    console.log('写入成功');
  } catch (err) {
    console.error('写入失败:', err);
  }
}

writeFile();
```

### 4. 追加内容

```javascript
const fs = require('fs').promises;

async function appendFile() {
  try {
    await fs.appendFile('./file.txt', '\n追加的内容', 'utf-8');
    console.log('追加成功');
  } catch (err) {
    console.error('追加失败:', err);
  }
}

appendFile();
```

### 5. 删除文件

```javascript
const fs = require('fs').promises;

async function deleteFile() {
  try {
    await fs.unlink('./file.txt');
    console.log('删除成功');
  } catch (err) {
    console.error('删除失败:', err);
  }
}

deleteFile();
```

### 6. 检查文件是否存在

```javascript
const fs = require('fs').promises;

async function checkFile() {
  try {
    await fs.access('./file.txt');
    console.log('文件存在');
  } catch (err) {
    console.log('文件不存在');
  }
}

checkFile();
```

---

## 🔗 知识关联

- **Node.js 模块系统**：fs 是 Node.js 内置模块，无需安装
- **异步编程**：fs 操作通常是异步的，建议使用 Promise 版本配合 async/await
- **HTTP 服务器**：在搭建静态文件服务器时，需要使用 fs 读取文件
- **路径处理**：常与 `path` 模块配合使用，处理文件路径

---

## 💻 练习任务

### 练习 1：读取并显示文件
1. 创建一个 `data.txt` 文件，写入一些内容
2. 编写代码读取该文件并打印内容

### 练习 2：写入日志文件
1. 创建一个日志系统，将时间戳和消息写入 `log.txt`
2. 每次运行程序都追加新的日志记录

### 练习 3：文件操作工具
编写一个工具函数：
- 如果文件不存在则创建并写入初始内容
- 如果文件存在则读取内容

### 练习 4：备份文件
1. 读取 `source.txt`
2. 将内容写入 `backup.txt`
3. 删除原文件（可选，慎用！）

---

## 📚 延伸阅读

### 相关概念
- **文件权限**：了解文件读写权限的概念
- **流（Stream）**：处理大文件时使用流式读写更高效
- **目录操作**：`fs.mkdir()`、`fs.readdir()` 等

### 下一步学习
- [ ] Node.js HTTP 服务器
- [ ] Stream 流式处理
- [ ] Express.js 静态文件服务

### 推荐资源
- [Node.js fs 文档](https://nodejs.org/api/fs.html)
- [JavaScript async/await 教程](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/async_function)

---

## 📝 学习笔记
<!-- 在这里添加你的学习心得和疑问 -->

---

## ✅ 完成情况
- [ ] 完成练习 1
- [ ] 完成练习 2
- [ ] 完成练习 3
- [ ] 完成练习 4

---

## 🧩 测验问题
<!-- 晚上 10 点会在这里追加测验问题 -->
