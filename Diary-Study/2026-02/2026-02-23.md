# 学习笔记 - 2026-02-23

## 主题：npm 包管理

### 核心知识点

#### 1. npm 简介
```bash
npm --version            # 查看 npm 版本
npm --help              # 查看 npm 帮助
```
- npm (Node Package Manager) 是 Node.js 的包管理器
- 全球最大的开源库生态系统
- 自动管理依赖关系，解决版本冲突

#### 2. install - 安装包

**本地安装（默认）：**
```bash
npm install <包名>           # 安装到 node_modules，添加到 dependencies
npm install <包名> --save    # 等同于上面（npm 5+ 默认行为）
npm i <包名>                 # 简写形式
```

**全局安装：**
```bash
npm install -g <包名>        # 全局安装，可在任何目录使用
npm install --global <包名>  # 同上
```

**安装特定版本：**
```bash
npm install <包名>@1.2.3     # 安装指定版本
npm install <包名>@latest    # 安装最新版本
npm install <包名>@next      # 安装下一个版本
```

**开发依赖：**
```bash
npm install <包名> --save-dev     # 安装并添加到 devDependencies
npm install <包名> -D             # 简写形式
```

#### 3. uninstall - 卸载包

**本地卸载：**
```bash
npm uninstall <包名>         # 卸载本地包
npm remove <包名>            # 同上
npm un <包名>                # 简写形式
```

**全局卸载：**
```bash
npm uninstall -g <包名>      # 卸载全局包
```

#### 4. package.json - 项目配置文件

**初始化项目：**
```bash
npm init                    # 交互式创建 package.json
npm init -y                 # 使用默认值快速创建
npm init --yes              # 同上
```

**字段说明：**
```json
{
  "name": "my-project",
  "version": "1.0.0",
  "description": "项目描述",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  },
  "keywords": ["node", "express"],
  "author": "Your Name",
  "license": "MIT"
}
```

**依赖字段区别：**
- `dependencies`：生产环境必需的包（运行时依赖）
- `devDependencies`：开发环境需要的包（测试工具、构建工具等）

#### 5. scripts - 脚本命令

**运行脚本：**
```bash
npm run <脚本名>             # 运行自定义脚本
npm start                   # 运行 start 脚本（简写）
npm test                    # 运行 test 脚本（简写）
```

**常用脚本示例：**
```json
{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "webpack --mode production",
    "test": "jest",
    "lint": "eslint .",
    "format": "prettier --write ."
  }
}
```

**脚本钩子：**
```json
{
  "scripts": {
    "preinstall": "echo '准备安装依赖...'",
    "postinstall": "echo '依赖安装完成！'",
    "prestart": "npm run build",
    "start": "node server.js"
  }
}
```

#### 6. 其他常用命令

**更新包：**
```bash
npm update                  # 更新所有包
npm update <包名>           # 更新指定包
npm outdated                # 查看过期的包
```

**查看包信息：**
```bash
npm list                    # 列出已安装的包
npm ls                      # 同上（简写）
npm info <包名>             # 查看包的详细信息
npm view <包名> versions    # 查看包的所有版本
```

**清理缓存：**
```bash
npm cache clean --force     # 清理 npm 缓存
```

### 常用场景

#### 场景1：初始化新项目
```bash
mkdir my-project && cd my-project
npm init -y
npm install express
```

#### 场景2：安装开发工具
```bash
npm install -D jest eslint prettier
```

#### 场景3：使用全局工具
```bash
npm install -g create-react-app
create-react-app my-app
```

#### 场景4：迁移项目依赖
```bash
# 复制 package.json 到新项目
cd new-project
npm install                 # 自动安装所有依赖
```

### 最佳实践

1. **使用 .gitignore**：
   ```
   node_modules/
   package-lock.json（可选）
   .npm/
   ```

2. **版本锁定**：
   - `package-lock.json` 锁定精确版本
   - 提交到 Git，确保团队使用相同版本

3. **依赖管理原则**：
   - 定期运行 `npm outdated` 查看过期包
   - 谨慎更新大版本（如 4.x → 5.0）
   - 使用 `npm audit` 检查安全漏洞

4. **脚本命名规范**：
   - 使用动词：`start`、`build`、`test`、`deploy`
   - 避免与 npm 内置命令冲突

### 关联知识点

- **语义化版本 (SemVer)**：`^` 和 `~` 的区别
  - `^1.2.3`：兼容 1.x.x（1.2.3 ≤ 版本 < 2.0.0）
  - `~1.2.3`：兼容 1.2.x（1.2.3 ≤ 版本 < 1.3.0）
- **npm registry**：npm 官方包仓库（registry.npmjs.org）
- **node_modules 结构**：依赖嵌套和扁平化

### 练习题

1. 创建一个新的 Node.js 项目，安装 Express 并配置启动脚本
2. 安装 Jest 作为开发依赖，编写一个简单的测试脚本
3. 使用 npm scripts 实现一个构建流程：lint → test → build

### 延伸阅读

- [npm 官方文档](https://docs.npmjs.com/)
- [package.json 完整字段说明](https://docs.npmjs.com/cli/v9/configuring-npm/package-json)
- [语义化版本规范](https://semver.org/lang/zh-CN/)

---

**学习时间**：2026-02-23 14:00
**状态**：测验等待中
**预计测验时间**：22:00

---

## 📝 测验问题

### 问题1
**dependencies 和 devDependencies 的主要区别是什么？**
- A. dependencies 用于全局包，devDependencies 用于本地包
- B. dependencies 是生产环境必需的包，devDependencies 是开发环境需要的包
- C. dependencies 需要手动安装，devDependencies 自动安装
- D. 没有区别，可以互换使用

### 问题2
**以下哪个命令会将包安装为开发依赖？**
- A. `npm install <包名>`
- B. `npm install <包名> -g`
- C. `npm install <包名> --save-dev`
- D. `npm install <包名> --save`

### 问题3
**语义化版本中，`^1.2.3` 表示什么范围？**
- A. 仅安装 1.2.3 版本
- B. 兼容 1.2.x（1.2.3 ≤ 版本 < 1.3.0）
- C. 兼容 1.x.x（1.2.3 ≤ 版本 < 2.0.0）
- D. 兼容所有版本（1.2.3 ≤ 版本 < ∞）

---

**测验发布时间**：2026-02-23 22:00
**请回复答案（如：1B 2C 3C）**
