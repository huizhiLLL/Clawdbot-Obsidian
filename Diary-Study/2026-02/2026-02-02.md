# 2026-02-02 - JS 变量声明（let/const/var）

📚 今日学习主题：JS 变量声明（let/const/var）

---

📌 核心知识点

### 1. var 的特点（传统方式）
```javascript
var name = "会枝";
console.log(name); // "会枝"
```

**问题：**
- 函数作用域，不是块级作用域
- 可以重复声明
- 存在变量提升（hoisting）

```javascript
// 问题示例：变量提升
console.log(x); // undefined（不是报错）
var x = 10;

// 问题示例：块级作用域失效
for (var i = 0; i < 5; i++) {
    console.log(i);
}
console.log(i); // 5（可以访问到！）
```

### 2. let - 现代推荐方式
```javascript
let name = "会枝";
console.log(name); // "会枝"
```

**优势：**
- 块级作用域（只在 {} 内有效）
- 不能重复声明
- 不会变量提升（TDZ - 暂时性死区）

```javascript
// 块级作用域示例
for (let i = 0; i < 5; i++) {
    console.log(i);
}
console.log(i); // ReferenceError: i is not defined

// 不能重复声明
let x = 10;
let x = 20; // SyntaxError: Identifier 'x' has already been declared
```

### 3. const - 常量声明
```javascript
const API_KEY = "sk-123456";
const PI = 3.14159;
```

**规则：**
- 必须初始化（声明时赋值）
- 不能重新赋值
- 块级作用域
- 对象/数组的属性可以修改（不能改变引用）

```javascript
// 必须初始化
const x; // SyntaxError: Missing initializer in const declaration

// 不能重新赋值
const PI = 3.14;
PI = 3.15; // TypeError: Assignment to constant variable

// 对象/数组的属性可以修改
const user = { name: "会枝", age: 20 };
user.age = 21; // ✅ 可以
user.city = "上海"; // ✅ 可以
user = {}; // ❌ 不能改变引用
```

### 4. 最佳实践总结

| 场景 | 推荐 |
|------|------|
| 常量、配置项 | `const` |
| 会重新赋值的变量 | `let` |
| 不再使用 `var` | ❌ 避免 |

```javascript
// ✅ 好的写法
const config = { api: "https://api.example.com", timeout: 5000 };
let counter = 0;
counter++;
```

---

## 🎯 与个人背景/项目/热点的关联

### 你的场景
你在开发 Vue3 + Node.js 的全栈项目，变量声明是代码基础：

```vue
<script setup>
// Vue3 Composition API - 推荐 const
const props = defineProps(['title']);
const emit = defineEmits(['update']);

// 响应式数据 - 推荐 ref + const
const count = ref(0);
const user = ref({ name: "会枝", age: 20 });

// 计算属性 - 推荐 const
const doubleCount = computed(() => count.value * 2);
</script>
```

```javascript
// Node.js 后端
const express = require('express'); // 第三方模块：const
const app = express(); // 应用实例：const
const PORT = 3000; // 配置项：const

let requestCount = 0; // 需要变化的：let

app.get('/', (req, res) => {
    requestCount++; // 累加
    res.send(`请求数：${requestCount}`);
});
```

### 应用示例

**你的魔方项目场景：**
```javascript
// 常量配置
const CUBE_SIZE = 3; // 3阶魔方
const COLORS = ['white', 'yellow', 'red', 'orange', 'green', 'blue'];

// 需要变化的
let moves = []; // 记录打乱步骤
let isSolving = false; // 解决状态

// 对象引用可以修改
const state = {
    faces: initFaces(), // 初始化
    moves: 0
};

state.moves = 10; // ✅ 可以修改属性
// state = {}; // ❌ 不能改变引用
```

---

## 🚀 快速练习（15 分钟）

### 练习 1：修复代码
下面的代码有什么问题？请修复：

```javascript
var x = 10;
for (var i = 0; i < 3; i++) {
    var x = i * 2;
}
console.log(x); // 输出什么？
```

**答案：**
输出 `4`。问题：`var x` 在循环内重复声明，且因为函数作用域，`i` 变量也会泄漏到循环外。

**修复：**
```javascript
const x = 10; // 外部用 const
for (let i = 0; i < 3; i++) {
    const doubled = i * 2; // 用不同变量名
}
console.log(x); // 10
```

### 练习 2：判断输出
以下代码会输出什么？

```javascript
const arr = [1, 2, 3];
arr.push(4);
arr = [5, 6, 7];
console.log(arr);
```

**答案：**
`TypeError: Assignment to constant variable`。因为 `arr = [5, 6, 7]` 尝试改变引用，但 `const` 不允许。

**修正：**
```javascript
const arr = [1, 2, 3];
arr.push(4); // ✅ 可以修改属性
arr.splice(0, 1); // ✅ 可以调用方法
console.log(arr); // [2, 3, 4]
```

### 练习 3：Vue3 响应式练习
在 Vue3 中，以下写法是否正确？

```javascript
const count = 0;
const increment = () => {
    count++;
};
```

**答案：**
❌ 错误。`const count = 0` 是原始值，不能修改。需要使用 `ref`：

```javascript
const count = ref(0);
const increment = () => {
    count.value++; // ✅ 修改 .value
};
```

---

## 📚 延伸阅读

1. [MDN: let](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/let)
2. [MDN: const](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/const)
3. [JavaScript 变量提升](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Grammar_and_types#变量提升)

---

## 💡 学习建议

1. **5 分钟**：理解 let/const 的区别（重点：作用域和可变性）
2. **5 分钟**：完成练习题，修复问题代码
3. **5 分钟**：在你的项目中检查是否有 `var`，替换为 `let/const`
4. **晚上 10 点**：我会问你的学习进度

---

## 🤔 每日测验（晚上 22:00）

### 1. 今天学了什么主题？
[请在此填写]

### 2. 有什么收获？
[请在此填写]

### 3. 有什么疑问吗？
[请在此填写]

### 4. 完成了 demo 吗？
[请在此填写]

---

**📝 填写完请回复"填完了"，我会检查你的答案并更新总结。**
