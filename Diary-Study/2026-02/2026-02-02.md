# 2026-02-02 - JS 数据类型

📚 今日学习主题：JS 数据类型

---

📌 核心知识点

### 1. 数据类型概览

JavaScript 有 **8 种** 数据类型，分为两大类：

**基本类型（7 种）：**
- `string` - 字符串
- `number` - 数字
- `boolean` - 布尔值
- `undefined` - 未定义
- `null` - 空值
- `symbol` - 符号（ES6）
- `bigint` - 大整数（ES2020）

**引用类型（1 种）：**
- `object` - 对象（包括数组、函数、日期等）

---

### 2. 基本类型详解

#### string - 字符串
```javascript
const name = "会枝";
const greeting = 'Hello';
const template = `你好，${name}`; // 模板字符串

// 常用方法
name.length; // 2
name.toUpperCase(); // "会枝"
name.includes("会"); // true
```

#### number - 数字
```javascript
const age = 20;
const pi = 3.14159;
const negative = -10;

// 特殊值
const infinity = Infinity;
const notANumber = NaN; // Not a Number

// 类型检查
typeof age; // "number"
typeof NaN; // "number"（注意！）
```

#### boolean - 布尔值
```javascript
const isStudent = true;
const hasJob = false;

// 布尔转换
Boolean(1); // true
Boolean(0); // false
Boolean(""); // false
Boolean("hello"); // true
```

#### undefined vs null
```javascript
// undefined - 变量已声明但未赋值
let x;
console.log(x); // undefined

// null - 明确赋值为空
let y = null;
console.log(y); // null

// 区别
typeof undefined; // "undefined"
typeof null; // "object"（历史遗留问题）
```

#### symbol & bigint（了解即可）
```javascript
// Symbol - 创建唯一标识
const id1 = Symbol("id");
const id2 = Symbol("id");
console.log(id1 === id2); // false

// BigInt - 处理大整数
const bigNum = 9007199254740991n;
const bigger = bigNum + 1n;
```

---

### 3. 引用类型详解

#### object - 对象
```javascript
const user = {
  name: "会枝",
  age: 20,
  major: "机械工程"
};

// 访问属性
user.name; // "会枝"
user["age"]; // 20

// 修改属性
user.age = 21;

// 添加属性
user.city = "上海";
```

#### array - 数组
```javascript
const colors = ["red", "green", "blue"];

// 数组是特殊的对象
typeof colors; // "object"
Array.isArray(colors); // true

// 常用方法
colors.length; // 3
colors.push("yellow"); // 添加
colors.pop(); // 删除最后一个
```

---

### 4. 类型检查方法

| 方法 | 说明 | 示例 |
|------|------|------|
| `typeof` | 检查基本类型 | `typeof 42` → `"number"` |
| `Array.isArray()` | 检查数组 | `Array.isArray([1,2])` → `true` |
| `instanceof` | 检查对象类型 | `[] instanceof Array` → `true` |
| `Object.prototype.toString` | 精确检查 | `toString.call(null)` → `"[object Null]"` |

```javascript
// typeof 示例
typeof "hello"; // "string"
typeof 42; // "number"
typeof true; // "boolean"
typeof undefined; // "undefined"
typeof null; // "object"（注意陷阱！）
typeof {}; // "object"
typeof []; // "object"
typeof function(){}; // "function"

// 更精确的检查
Object.prototype.toString.call(null); // "[object Null]"
Object.prototype.toString.call([]); // "[object Array]"
```

---

### 5. 类型转换

```javascript
// 字符串转换
String(123); // "123"
String(true); // "true"
123 + ""; // "123"（隐式转换）

// 数字转换
Number("123"); // 123
Number("hello"); // NaN
parseInt("123px"); // 123
parseFloat("3.14"); // 3.14

// 布尔转换
Boolean(""); // false
Boolean(" "); // true（空字符串转 false，非空转 true）
Boolean(0); // false
Boolean(-1); // true（非零数字转 true）
Boolean(null); // false
Boolean(undefined); // false
Boolean({}); // true（对象总是 true）
```

---

## 🎯 与个人背景/项目/热点的关联

### 你的场景

**Vue3 组件中：**
```vue
<script setup>
// 响应式数据 - 使用 ref 接受任何类型
const count = ref(0); // number
const message = ref(""); // string
const isLoading = ref(false); // boolean
const userInfo = ref(null); // null（初始为空）

const fetchUser = async () => {
  isLoading.value = true;
  const response = await fetch('/api/user');
  userInfo.value = await response.json(); // object
  isLoading.value = false;
};

// 检查类型
if (typeof userInfo.value === 'object' && userInfo.value !== null) {
  console.log(userInfo.value.name);
}
</script>
```

**Node.js API 中：**
```javascript
app.get('/api/cube', (req, res) => {
  const { size, speed } = req.query; // string（从 URL 获取）

  // 类型转换
  const cubeSize = parseInt(size) || 3;
  const cubeSpeed = parseFloat(speed) || 1.0;

  // 类型检查
  if (isNaN(cubeSize) || cubeSize < 2 || cubeSize > 10) {
    return res.status(400).json({ error: "Invalid size" });
  }

  res.json({ size: cubeSize, speed: cubeSpeed });
});
```

**魔方项目中：**
```javascript
// 魔方状态
const cubeState = {
  faces: [
    ["white", "white", "white"], // 二维数组
    ["yellow", "yellow", "yellow"],
    // ... 其他面
  ],
  moves: [], // 数组
  isSolving: false // 布尔值
};

// 检查状态
if (cubeState.moves.length > 0 && !cubeState.isSolving) {
  console.log(`已完成 ${cubeState.moves.length} 步`);
}
```

---

## 🚀 快速练习（15 分钟）

### 练习 1：判断类型
以下代码的输出是什么？

```javascript
console.log(typeof undefined);
console.log(typeof null);
console.log(typeof []);
console.log(typeof function(){});
console.log(typeof NaN);
```

**答案：**
```
undefined
object
object
function
number
```

---

### 练习 2：类型转换陷阱
预测输出：

```javascript
console.log(1 + "2"); // ?
console.log("1" - "2"); // ?
console.log("1" * "2"); // ?
console.log("1" / "2"); // ?
console.log("1" + 1); // ?
console.log(true + true); // ?
console.log("" - 0); // ?
```

**答案：**
```
"12"  // 数字 + 字符串 = 字符串拼接
-1    // 字符串减法会转为数字
2     // 字符串乘法会转为数字
0.5   // 字符串除法会转为数字
2     // 字符串 + 数字 = 字符串拼接
2     // true 转为 1，所以 1 + 1 = 2
0     // 空字符串转为 0
```

---

### 练习 3：类型检查函数
写一个函数，检查变量是否为有效的数组：

```javascript
function isArray(value) {
  // 请填写代码
}
```

**答案：**
```javascript
function isArray(value) {
  return Array.isArray(value);
}

// 或者（不推荐，但能理解原理）
function isArray(value) {
  return Object.prototype.toString.call(value) === "[object Array]";
}
```

**测试：**
```javascript
console.log(isArray([1, 2, 3])); // true
console.log(isArray({})); // false
console.log(isArray("hello")); // false
```

---

## 📚 延伸阅读

1. [MDN - JavaScript 数据类型](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Data_structures)
2. [MDN - typeof](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/typeof)
3. [JavaScript 类型转换陷阱](https://javascript.info/type-conversions)

---

## 💡 学习建议

1. **5 分钟**：记住 8 种数据类型（7 种基本 + 1 种引用）
2. **5 分钟**：理解 `typeof` 的陷阱（特别是 `null` 和数组）
3. **5 分钟**：在项目中检查代码，是否有隐式类型转换的 bug
4. **最佳实践**：在处理 API 数据时，始终做类型检查

---

## 🤔 每日测验（晚上 22:00）

### 1. 今天学了什么主题？
[请在此填写]

### 2. JavaScript 有哪些数据类型？
[请在此填写]

### 3. typeof null 返回什么？为什么？
[请在此填写]

### 4. 有什么收获？
[请在此填写]

### 5. 有什么疑问吗？
[请在此填写]

### 6. 完成了练习吗？
[请在此填写]

---

**📝 填写完请回复"填完了"，我会检查你的答案并更新总结。**
