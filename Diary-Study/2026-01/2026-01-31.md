📚 今日学习主题：JS 函数基础 📚

---

📌 核心知识点

### 函数声明方式

```javascript
// 1. 函数声明（Function Declaration）
function greet(name) {
  console.log("Hello, " + name + "!");
}

// 2. 函数表达式（Function Expression）
const greet2 = function(name) {
  console.log("Hello, " + name + "!");
};

// 3. 箭头函数（Arrow Function）- ES6 推荐
const greet3 = (name) => {
  console.log("Hello, " + name + "!");
};

// 4. 简写形式（只有一个参数和一行语句）
const greet4 = name => "Hello, " + name + "!";
```

### 参数与返回值

```javascript
// 多个参数
function add(a, b) {
  return a + b;
}

// 默认参数
function sayHello(name = "World") {
  console.log("Hello, " + name + "!");
}

// 剩余参数
function sumAll(...numbers) {
  return numbers.reduce((sum, num) => sum + num, 0);
}

console.log(sumAll(1, 2, 3, 4)); // 10
```

### 作用域与闭包（重要！）

```javascript
// 函数作用域
let globalVar = "全局变量";

function testScope() {
  let localVar = "局部变量";
  console.log(globalVar); // ✅ 可以访问
  console.log(localVar); // ✅ 可以访问
}

console.log(localVar); // ❌ 无法访问，局部变量在函数外不存在

// 闭包示例
function createCounter() {
  let count = 0;
  return function() {
    count++;
    console.log(count);
    return count;
  };
}

const counter = createCounter();
counter(); // 1
counter(); // 2
counter(); // 3
// createCounter 的执行已经结束了，但 count 变量仍然被记住
```

---

## 🎯 与个人背景/项目/热点的关联

### 你平时可能遇到的问题

- vibe coding 时不清楚函数作用域，变量冲突
- 不理解箭头函数和普通函数的区别
- 不擅长使用闭包来保存状态

### 实际应用场景

```javascript
// 场景 1：魔方计时器 - 使用闭包保存状态
function createTimer() {
  let startTime = null;
  let intervalId = null;
  
  return {
    start: () => {
      startTime = Date.now();
      intervalId = setInterval(() => {
        console.log((Date.now() - startTime) / 1000);
      }, 1000);
    },
    stop: () => {
      clearInterval(intervalId);
      return (Date.now() - startTime) / 1000;
    }
  };
}

const timer = createTimer();
timer.start();
// ... 计时
const duration = timer.stop();
console.log("用时：" + duration + "秒");

// 场景 2：数组处理 - 使用回调函数
function processArray(arr, callback) {
  arr.forEach((item, index) => {
    callback(item, index);
  });
}

processArray([1, 2, 3], (item, index) => {
  console.log("索引 " + index + ": " + item);
});

// 场景 3：事件处理 - 箭头函数简化代码
document.getElementById('button').addEventListener('click', (event) => {
  console.log('按钮被点击', event.target);
});
```

---

## 🚀 快速练习（15 分钟）

### 练习 1：函数声明

用三种不同的方式（声明、表达式、箭头）写一个求平方的函数：

```javascript
// 方式 1
function square1(n) {
  // 你的代码
}

// 方式 2
const square2 = function(n) {
  // 你的代码
};

// 方式 3
const square3 = n => {
  // 你的代码
};
```

**答案：**

```javascript
// 方式 1
function square1(n) {
  return n * n;
}

// 方式 2
const square2 = function(n) {
  return n * n;
};

// 方式 3
const square3 = n => n * n;
```

### 练习 2：默认参数

写一个函数，如果没有传参数就返回默认值：

```javascript
function greet(name) {
  // 你的代码
}

console.log(greet());      // 应该输出 "Hello, World"
console.log(greet("会枝")); // 应该输出 "Hello, 会枝"
```

**答案：**

```javascript
function greet(name = "World") {
  return "Hello, " + name;
}
```

### 练习 3：闭包理解

下面的代码输出什么？为什么？

```javascript
function createAdder(n) {
  return function(m) {
    return n + m;
  };
}

const add5 = createAdder(5);
const add10 = createAdder(10);

console.log(add5(3));  // ??
console.log(add10(3)); // ??
```

**答案：**

- `add5(3)` 输出 `8`（5 + 3）
- `add10(3)` 输出 `13`（10 + 3）

**原因：** `createAdder` 返回的闭包函数记住了参数 `n` 的值

---

## 📚 延伸阅读

1. [MDN - 函数](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Functions)
2. [ES6 箭头函数详解](https://es6.ruanyif.com/#docs/function-basics)
3. [JavaScript 闭包深入理解](https://zh.javascript.info/closure)

---

## 💡 学习建议

1. **5 分钟**：阅读上面的核心知识点
2. **5 分钟**：完成三个练习题
3. **5 分钟**：打开你以前写的代码，找找有没有函数可以重构的
4. **晚上 10 点**：我会问你的学习进度，准备好回答！

---

## 🤔 每日测验（晚上 22:00）

### 1. 今天学了什么主题？
[请在此填写]

### 2. 有什么收获？
[请在此填写]

### 3. 有什么疑问吗？
[请在此填写]

### 4. 完成了 demo 吗？
[请在此填写]

---

**📝 填写完请回复"填完了"，我会检查你的答案并更新总结。**
