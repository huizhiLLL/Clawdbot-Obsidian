📚 今日学习主题：JS 数据类型 📚

📌 核心知识点

### JS 数据类型分类

JavaScript 有 7 种基本类型和 1 种引用类型：

```javascript
// 基本类型（Primitive Types）

// 1. string - 字符串
let name = "会枝";
let message = 'Hello World';

// 2. number - 数字
let age = 20;
let price = 99.99;
let infinity = Infinity;

// 3. boolean - 布尔值
let isStudent = true;
let isWorking = false;

// 4. null - 空值（表示"没有值"）
let emptyValue = null;

// 5. undefined - 未定义（声明了但没赋值）
let notDefined;
console.log(notDefined); // undefined

// 6. symbol - 符号（ES6 新增，用于对象属性）
let uniqueId = Symbol('id');

// 7. bigint - 大整数（ES2020 新增）
let bigNumber = 9007199254740991n;

// 引用类型（Reference Types）

// 8. object - 对象（包括数组、函数）
let person = { name: "会枝", age: 20 };
let scores = [95, 87, 92];
let greet = function() { console.log("Hello"); };
```

### 基本类型 vs 引用类型（重要！）

| 特性   | 基本类型 | 引用类型 |
| ---- | ---- | ---- |
| 存储方式 | 值存储  | 引用存储 |
| 赋值时  | 复制值  | 复制引用 |
| 比较时  | 比较值  | 比较引用 |

```javascript
// 基本类型 - 值复制
let a = 10;
let b = a; // b 得到的是 10 的副本
a = 20; // 不影响 b
console.log(b); // 10 ✅

// 引用类型 - 引用复制
let arr1 = [1, 2, 3];
let arr2 = arr1; // arr2 指向同一个数组
arr1.push(4); // 会影响 arr2
console.log(arr2); // [1, 2, 3, 4] ⚠️

// 正确的数组复制方式
let arr3 = [1, 2, 3];
let arr4 = [...arr3]; // 或 arr4 = arr3.slice()
arr3.push(4);
console.log(arr4); // [1, 2, 3] ✅
```

---

## 🎯 与个人背景/项目/热点的关联

### 你平时可能遇到的问题

- vibe coding 时不小心修改了数组，结果影响到了其他地方
- 不理解为什么 `console.log(obj)` 显示的内容和预期不一样
- 混淆 `null` 和 `undefined` 的用法

### 实际应用场景

```javascript
// 场景 1：复制数组避免副作用
let scores1 = [95, 87, 92];
let bestScores = [...scores1]; // 复制一份
bestScores.push(100);
console.log(scores1); // [95, 87, 92] ✅ 原数组不变

// 场景 2：检查变量类型
let value = "123";
if (typeof value === "number") {
  console.log("是数字");
} else if (typeof value === "string") {
  console.log("是字符串");
}

// 场景 3：检查是否为 null 或 undefined
let user = null;
if (user === null || user === undefined) {
  console.log("用户不存在");
}
// 简写：
if (user == null) { // null 和 undefined 都会被 == null 捕获
  console.log("用户不存在");
}
```

---

## 🚀 快速练习（15 分钟）

### 练习 1：说出类型

下面的变量分别是什么类型？

```javascript
let a = "Hello";
let b = 123;
let c = true;
let d = null;
let e = undefined;
let f = [1, 2, 3];
let g = { name: "会枝" };
```

**答案：**

- a: string
- b: number
- c: boolean
- d: object（typeof null 返回 "object"，这是 JS 的历史遗留 bug）
- e: undefined
- f: object（数组是特殊的对象）
- g: object

### 练习 2：修复 bug

下面的代码为什么输出不对？如何修复？

```javascript
let list1 = ["apple", "banana"];
let list2 = list1;
list2.push("cherry");
console.log(list1); // 想要输出 ["apple", "banana"]，但实际是 ["apple", "banana", "cherry"]
```

**答案：**

- 原因：list2 复制的是引用，不是值，所以修改 list2 会影响 list1
- 修复：`let list2 = [...list1];` 或 `let list2 = list1.slice();`

### 练习 3：类型检查

写一个函数，检查变量是否是数组：

```javascript
function isArray(value) {
  // 你的代码
}

console.log(isArray([1, 2, 3])); // 应该输出 true
console.log(isArray("hello")); // 应该输出 false
console.log(isArray({ a: 1 })); // 应该输出 false
```

**答案：**

```javascript
function isArray(value) {
  return Array.isArray(value);
}
// 或者：
function isArray(value) {
  return Object.prototype.toString.call(value) === "[object Array]";
}
```

---

## 📚 延伸阅读

1. [MDN - JavaScript 数据类型](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Data_structures)
2. [值 vs 引用 - 深入理解](https://zh.javascript.info/object-copy)
3. [typeof vs instanceof - 类型检查最佳实践](https://javascript.info/type-conversions#typeof)

---

## 💡 学习建议

1. **5 分钟**：阅读上面的核心知识点
2. **5 分钟**：完成三个练习题
3. **5 分钟**：打开你以前写的代码，找找有没有引用类型复制的问题
4. **晚上 10 点**：我会问你的学习进度，准备好回答！

---

## 🤔 每日测验（晚上 22:00）

### 1. 今天学了什么主题？
[请在此填写]

### 2. 有什么收获？
[请在此填写]

### 3. 有什么疑问吗？
[请在此填写]

### 4. 完成了 demo 吗？
[请在此填写]

---

**📝 填写完请回复"填完了"，我会检查你的答案并更新总结。**
